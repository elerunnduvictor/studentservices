/* ═══════════════ OKR PROGRESS — DATA REMOVED ═══════════════

   This file held 48 sub-key results: the objective, the key result, who the
   stakeholder and project manager are, the goal, the progress against it, the
   status and the free-text comment on each.

   It was the offline fallback, loaded on demand by shared/js/data-service.js.
   Being a static file on a public site, it was fetchable by URL with no
   sign-in — the same rows `v_hub_okrs` used to serve anonymously, and now
   does not.

   The OKR pages read v_hub_okrs only. OKR_COLORS and STATUS_COLORS, which this
   file also defined, live in shared/js/hub-palette.js and are loaded by every
   page that needs them — they are presentation constants, not anyone's data,
   which is why they were moved there in the first place.

   Nothing loads this file any more. It is left in place as a record of what
   was here and why it went, so it does not get regenerated.

   See supabase/rls-lockdown.sql for the other half of the same fix.
   ═══════════════════════════════════════════════════════════ */
